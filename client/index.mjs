import client from './index.js';

export const {
  IAgentCreateOptions,
  ICoreConnectionOptions,
  Handler,
  Agent,
  LocalCoreConnection,
  RemoteCoreConnection,
} = client;

export default client.default;
