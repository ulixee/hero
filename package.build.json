{
  "scripts": {
    "test": "cross-env NODE_ENV=test HERO_DATA_DIR=.sessions-test jest"
  },
  "devDependencies": {
    "jest": "^26.6.3",
    "jest-summary-reporter": "^0.0.2",
    "jest-environment-node": "^26.6.2",
    "cross-env": "^7.0.2"
  }
}
