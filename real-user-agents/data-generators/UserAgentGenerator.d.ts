import { IRealUserAgentsData } from '../data';
export default class UserAgentGenerator {
    static run(data: IRealUserAgentsData): Promise<void>;
    private static getAllPatches;
    private static getStablePatches;
}
